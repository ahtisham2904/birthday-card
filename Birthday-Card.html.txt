<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Happy Birthday!</title>
  <style>
    /* Modern Aesthetic Styling */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      text-align: center;
    }

    .card {
      background: rgba(255, 255, 255, 0.9);
      padding: 2rem 3rem;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      position: relative;
      max-width: 500px;
      z-index: 10;
      backdrop-filter: blur(5px);
    }

    h1 {
      color: #ff6b6b;
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
    }

    p {
      color: #555;
      font-size: 1.2rem;
      line-height: 1.6;
    }

    .signature {
      font-family: 'Brush Script MT', cursive;
      font-size: 1.5rem;
      color: #ff6b6b;
      margin-top: 1.5rem;
    }

    /* Balloons Animation */
    .balloon {
      position: absolute;
      width: 30px;
      height: 40px;
      background: #ff6b6b;
      border-radius: 50%;
      animation: float 6s ease-in-out infinite;
      opacity: 0.7;
    }

    .balloon:before {
      content: "";
      position: absolute;
      width: 1px;
      height: 15px;
      background: #333;
      top: 40px;
      left: 15px;
    }

    .balloon:nth-child(2) {
      background: #74b9ff;
      left: 15%;
      top: 30%;
      animation-delay: 1s;
    }

    .balloon:nth-child(3) {
      background: #55efc4;
      left: 70%;
      top: 20%;
      animation-delay: 2s;
    }

    .balloon:nth-child(4) {
      background: #ffeaa7;
      left: 80%;
      top: 60%;
      animation-delay: 0.5s;
    }

    .balloon:nth-child(5) {
      background: #a29bfe;
      left: 25%;
      top: 70%;
      animation-delay: 3s;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0) rotate(5deg); }
      50% { transform: translateY(-20px) rotate(-5deg); }
    }

    /* Heart Confetti (on hover) */
    .card:hover .heart {
      display: block;
    }

    .heart {
      position: absolute;
      display: none;
      color: #ff6b6b;
      font-size: 1rem;
      animation: heart-float 3s ease-in-out forwards;
    }

    @keyframes heart-float {
      0% { transform: translateY(0) rotate(0); opacity: 1; }
      100% { transform: translateY(-100px) rotate(360deg); opacity: 0; }
    }
  </style>
</head>
<body>
  <!-- Floating Balloons -->
  <div class="balloon"></div>
  <div class="balloon"></div>
  <div class="balloon"></div>
  <div class="balloon"></div>
  <div class="balloon"></div>

  <!-- Birthday Card -->
  <div class="card" id="card">
    <h1>Happy Birthday! 🎉</h1>
    <p>Wishing you a day filled with joy, laughter, and all the happiness in the world. May this year bring you endless blessings!</p>
    <p class="signature">From, [Your Name]</p>
  </div>

  <!-- Heart Confetti (JS) -->
  <script>
    const card = document.getElementById('card');
    card.addEventListener('mouseover', () => {
      for (let i = 0; i < 20; i++) {
        createHeart();
      }
    });

    function createHeart() {
      const heart = document.createElement('div');
      heart.classList.add('heart');
      heart.innerHTML = '❤️';
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = Math.random() * 2 + 3 + 's';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 3000);
    }
  </script>
</body>
</html>